name: somospie
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - gdal
  - pip
  - pip:
      - pandas
      - pyspark
      - findspark
      - scikit-learn
      - scikit-image
      - matplotlib
      - grass-session
      - bokeh
      - xmltodict
      - requests
      - colorcet
      - geopandas
      - tqdm
      - panel
      - pyvista 
      - vtk 
      - jupyter
      # - openvisus